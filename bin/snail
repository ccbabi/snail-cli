#!/usr/bin/env node

const program = require('commander')
const pkg = require('../package')

program
  .version(pkg.version)
  .usage('<command> <option>')
  .command('init', '初始化一个项目')
  .command('mock', 'Mock服务器')
  .parse(process.argv)
